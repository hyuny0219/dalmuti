# 달무티(The Great Dalmuti) 온라인 웹 게임 구현 계획

## 1. 프로젝트 개요

보드게임 **달무티**를 여러 사용자가 브라우저에서 실시간으로 함께 즐길 수 있는 웹 게임으로 구현한다.

- **플레이 인원**: 4~8명 (권장 5~6명) — 부족한 인원은 **난이도 선택 가능한 AI 봇**으로 충원
- **플랫폼**: 웹 브라우저 (데스크톱 + 모바일 반응형)
- **핵심 특징**: 실시간 멀티플레이, 방(로비) 시스템, 서버 권위(server-authoritative) 게임 로직

---

## 2. 게임 규칙 정리 (구현 대상 명세)

구현 전에 규칙을 명확히 정의해야 게임 로직의 요구사항이 된다.

### 2.1 카드 구성 (총 80장)

| 카드 숫자 | 이름 | 장수 |
|---|---|---|
| 1 | 달무티(Dalmuti) | 1장 |
| 2 | 대주교 | 2장 |
| 3 | 시종장 | 3장 |
| ... | ... | 숫자 = 장수 |
| 12 | 농노 | 12장 |
| 13 (조커) | 광대(Jester) | 2장 |

- 숫자가 **낮을수록 강한** 카드
- 광대(조커)는 아무 숫자로도 사용 가능한 와일드카드. 단독으로 낼 때는 13으로 취급

### 2.2 라운드 진행

1. 전체 카드를 모든 플레이어에게 분배 (인원에 따라 장수 차이 발생 가능)
2. 선(리드) 플레이어가 **같은 숫자의 카드 N장**을 낸다 (광대를 와일드로 섞을 수 있음)
3. 다음 플레이어는 **같은 장수(N장)** 이면서 **더 낮은 숫자**의 조합을 내거나 패스
4. 모든 플레이어가 연속으로 패스하면 마지막으로 낸 사람이 새 리드를 가진다
5. 손패를 모두 털어낸 순서대로 다음 라운드의 계급이 결정된다

### 2.3 계급 시스템

| 순위 | 계급 |
|---|---|
| 1등 | 왕(Greater Dalmuti) |
| 2등 | 소달무티(Lesser Dalmuti) |
| 중간 | 상인(Merchant) |
| 꼴찌-1 | 소농노(Lesser Peon) |
| 꼴찌 | 대농노(Greater Peon) |

### 2.4 세금(Taxation) 단계

- 라운드 시작 시 **대농노 → 왕**에게 가장 좋은 카드 2장 상납, 왕은 원하는 카드 2장 반환
- **소농노 → 소달무티**에게 가장 좋은 카드 1장 상납, 소달무티는 1장 반환

### 2.5 혁명(Revolution)

- 세금 단계 전에 **광대 2장을 모두 가진 플레이어**는 혁명을 선언할 수 있다 → 해당 라운드 세금 면제
- 그 플레이어가 **대농노**라면 "대혁명" → 계급이 완전히 역전

> ⚠️ 하우스 룰 변형이 많으므로, 방 생성 시 옵션(혁명 사용 여부, 라운드 수, 시간제한 등)으로 설정 가능하게 설계한다.

---

## 3. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | **React 18 + TypeScript + Vite** | 빠른 개발, 타입 안정성, 생태계 |
| 상태관리 | **Zustand** | 소켓 이벤트 → 스토어 반영이 단순함 |
| 스타일링 | **Tailwind CSS** | 반응형 UI 빠른 구축 |
| 백엔드 | **Node.js + TypeScript + Socket.IO** | 실시간 양방향 통신 표준, 프론트와 언어 통일 |
| 게임 로직 | **공유 패키지 (shared)** | 서버·클라이언트가 타입과 규칙 검증 로직 공유 |
| 저장소 | MVP: **인메모리** → 확장 시 Redis | MVP에는 DB 불필요, 방/게임 상태는 휘발성 |
| 배포 | 프론트: Vercel/Netlify, 백엔드: Fly.io/Railway | 무료 티어로 시작 가능 |
| 테스트 | Vitest (로직 단위 테스트), Playwright (E2E) | |

### 모노레포 구조 (pnpm workspace)

```
dalmuti/
├── packages/
│   └── shared/          # 타입, 게임 규칙, 검증 로직 (서버/클라 공용)
│       ├── src/
│       │   ├── types.ts        # Card, Player, GameState, 이벤트 타입
│       │   ├── deck.ts         # 카드 생성/셔플/분배
│       │   ├── rules.ts        # 플레이 유효성 검증 (canPlay, canBeat)
│       │   └── constants.ts
├── apps/
│   ├── server/          # Socket.IO 서버
│   │   ├── src/
│   │   │   ├── index.ts        # 서버 엔트리
│   │   │   ├── room.ts         # 방 관리 (생성/입장/퇴장)
│   │   │   ├── game.ts         # 게임 상태 머신
│   │   │   └── handlers/       # 소켓 이벤트 핸들러
│   └── web/             # React 클라이언트
│       ├── src/
│       │   ├── pages/          # Home, Lobby, Game
│       │   ├── components/     # Card, Hand, PlayerSeat, PlayArea ...
│       │   ├── store/          # Zustand 스토어
│       │   └── socket/         # 소켓 연결/이벤트 바인딩
└── PLAN.md
```

---

## 4. 아키텍처 설계

### 4.1 핵심 원칙: 서버 권위 (Server-Authoritative)

- 모든 게임 상태(덱, 손패, 턴)는 **서버에만 존재**
- 클라이언트는 "행동 요청"만 보내고, 서버가 검증 후 결과를 브로드캐스트
- 각 플레이어에게는 **자기 손패만** 전송 (다른 플레이어는 장수만 공개) → 치팅 방지

### 4.2 게임 상태 머신

```
LOBBY (대기) → DEALING (분배) → REVOLUTION_CHECK (혁명 선언 대기)
→ TAXATION (세금 교환) → PLAYING (트릭 진행) → ROUND_END (계급 확정)
→ (다음 라운드: DEALING으로) 또는 GAME_END
```

### 4.3 소켓 이벤트 설계 (주요)

**클라이언트 → 서버**
| 이벤트 | 페이로드 | 설명 |
|---|---|---|
| `room:create` | `{ nickname, options }` | 방 생성 |
| `room:join` | `{ roomCode, nickname }` | 방 입장 |
| `room:start` | - | 방장이 게임 시작 |
| `room:addBot` | `{ difficulty }` | 방장이 AI 봇 추가 (easy/normal/hard) |
| `room:removeBot` | `{ botId }` | 방장이 봇 제거 |
| `room:setBotDifficulty` | `{ botId, difficulty }` | 봇 난이도 변경 |
| `game:declareRevolution` | `{ declare: boolean }` | 혁명 선언/포기 |
| `game:payTax` | `{ cardIds }` | 세금 카드 선택 |
| `game:play` | `{ cardIds }` | 카드 내기 |
| `game:pass` | - | 패스 |
| `chat:send` | `{ text }` | 채팅 메시지 전송 (로비/게임 공용) |

**서버 → 클라이언트**
| 이벤트 | 설명 |
|---|---|
| `room:state` | 방 인원/설정 변경 브로드캐스트 |
| `game:state` | 공개 게임 상태 (턴, 필드 카드, 각자 남은 장수, 계급) |
| `game:hand` | 개인 손패 (해당 플레이어에게만) |
| `game:event` | 애니메이션/로그용 이벤트 (누가 무엇을 냈는지 등) |
| `chat:message` | 채팅 메시지 브로드캐스트 (시스템 메시지 포함) |
| `error` | 잘못된 행동에 대한 사유 |

### 4.4 핵심 데이터 모델 (shared/types.ts)

```typescript
type Card = { id: string; rank: number };           // rank 1~12, 13 = Jester

type PlayerPublic = {
  id: string; nickname: string; handCount: number;
  rank: Rank | null;        // 계급
  finishedOrder: number | null;
  connected: boolean;
};

type GameState = {
  phase: 'LOBBY' | 'DEALING' | 'REVOLUTION' | 'TAXATION' | 'PLAYING' | 'ROUND_END' | 'GAME_END';
  players: PlayerPublic[];
  currentTurn: string | null;      // playerId
  fieldCards: Card[];              // 현재 필드에 깔린 조합
  fieldOwner: string | null;
  passCount: number;
  round: number;
  options: RoomOptions;
};

type ChatMessage = {
  id: string;
  type: 'user' | 'system';     // system: 입장/퇴장, 혁명 선언, 라운드 결과 등
  senderId: string | null;     // system 메시지는 null
  senderNickname: string | null;
  text: string;
  timestamp: number;
};
```

### 4.6 채팅 시스템 설계

채팅은 로비와 게임 화면에서 공통으로 사용하는 핵심 기능이다.

- **범위**: 방(room) 단위 채팅. 로비 대기 중과 게임 진행 중 모두 같은 채널을 사용
- **저장**: 방별 최근 메시지 N개(예: 100개)만 인메모리에 보관 — 재접속 시 최근 대화 복구용. 방이 사라지면 함께 소멸 (DB 불필요)
- **시스템 메시지**: 게임 이벤트(입장/퇴장, 게임 시작, 혁명 선언, "OO님이 1등!")를 채팅 스트림에 `system` 타입으로 섞어 보여줘 별도 게임 로그 UI를 겸한다
- **서버 측 검증**:
  - 메시지 길이 제한 (예: 200자), 빈 메시지 거부
  - 도배 방지 레이트 리밋 (예: 초당 2건, 초과 시 무시 + 경고)
  - 클라이언트 렌더링 시 텍스트로만 출력 (XSS 방지 — HTML 해석 금지)
- **UI**:
  - 데스크톱: 게임 화면 우측 사이드 패널 (접기/펼치기 가능)
  - 모바일: 하단 시트 또는 토글 오버레이 (게임 조작을 가리지 않게)
  - 새 메시지 도착 시 접힌 상태면 뱃지 표시
- **빠른 채팅(퀵챗)**: 자주 쓰는 문구 버튼("빨리요~", "굿플레이!", "ㅋㅋㅋ") — 모바일에서 타이핑 부담을 줄임

### 4.7 AI 봇 시스템 설계

인원이 부족할 때 방장이 로비에서 봇을 추가해 빈 자리를 채운다. 봇마다 **난이도를 개별 선택**할 수 있다.

**기본 구조**

- 봇은 **서버에서만 실행** — 일반 플레이어와 동일하게 게임 상태 머신의 참가자로 취급되고, 같은 규칙 엔진(`canPlay`/`canBeat`)으로 행동을 검증받는다
- 봇의 턴이 오면 서버가 난이도별 전략 함수를 호출해 행동을 결정. 사람처럼 보이도록 1~3초 랜덤 지연 후 실행
- 봇 인터페이스를 통일해 난이도별 구현체를 교체 가능하게 설계:

```typescript
type BotDifficulty = 'easy' | 'normal' | 'hard';

interface BotStrategy {
  decidePlay(view: BotGameView): Card[] | 'pass';   // 트릭에서 낼 카드 결정
  decideTax(view: BotGameView): Card[];              // 세금으로 낼/돌려줄 카드 결정
  decideRevolution(view: BotGameView): boolean;      // 혁명 선언 여부
}

// BotGameView: 봇이 볼 수 있는 정보만 담은 뷰 (자기 손패 + 공개 정보).
// 사람과 동일한 정보만 제공해 치팅 없는 공정한 봇을 보장
```

**난이도별 전략**

| 난이도 | 트릭 플레이 | 광대 활용 | 세금/혁명 | 구현 방식 |
|---|---|---|---|---|
| **easy (쉬움)** | 낼 수 있는 조합 중 무작위 선택, 낼 수 있어도 30% 확률로 패스 | 광대를 아끼지 않고 아무렇게나 섞어 냄 | 세금: 무작위 반환. 혁명: 선언 안 함 | 유효 수 목록에서 랜덤 |
| **normal (보통)** | 가장 약한(높은 숫자) 조합부터 소진, 강한 카드는 후반을 위해 보존 | 광대는 단독 조합을 완성할 때만 사용 | 세금: 가장 약한 카드 반환. 혁명: 대농노일 때만 선언(대혁명) | 휴리스틱 (탐욕 전략) |
| **hard (어려움)** | 카드 카운팅 — 이미 나온 카드를 추적해 내 조합이 잡힐 확률 계산, 리드를 잡기 위한 전략적 패스, 손패를 조합 단위로 나눠 최소 턴 수로 털어내는 플랜 수립 | 광대 2장의 기대가치를 계산해 와일드 사용 vs 13 단독 사용 판단 | 세금: 조합 유지에 기여 없는 카드 반환. 혁명: 계급/손패 상황에 따른 기대이익 판단 | 카운팅 + 시뮬레이션 기반 평가 |

**공정성 원칙**: 난이도가 높아져도 봇에게 비공개 정보(남의 손패)를 주지 않는다. 난이도 차이는 오직 **전략의 정교함**으로만 만든다.

**UI**

- 로비: 방장에게 "봇 추가" 버튼 → 난이도 선택 드롭다운. 봇은 참가자 목록에 난이도 뱃지(🟢 쉬움 / 🟡 보통 / 🔴 어려움)와 봇 아이콘으로 표시
- 봇 닉네임은 자동 생성 (예: "봇-광대", "봇-농노")
- 게임 중 플레이어가 이탈해 장시간 미복귀 시, 방장이 해당 자리를 봇으로 대체하는 옵션 제공 (게임 중단 방지)

### 4.5 재접속 처리

- 소켓 연결이 끊겨도 플레이어를 즉시 제거하지 않고 `connected: false`로 표시
- 세션 토큰(로컬스토리지)으로 재입장 시 기존 자리 복구
- 일정 시간(예: 60초) 미복귀 시 자동 패스 처리 또는 방장이 강퇴 가능

---

## 5. 단계별 구현 로드맵

### Phase 1 — 프로젝트 셋업 + 게임 코어 로직 (가장 중요)
- [ ] pnpm 모노레포, TypeScript, ESLint/Prettier 설정
- [ ] `shared` 패키지: 카드 타입, 덱 생성/셔플/분배
- [ ] 규칙 엔진: `canPlay()`, `canBeat()` — 광대 와일드카드 포함 유효성 검증
- [ ] 라운드 진행 상태 머신 (순수 함수로, UI 없이)
- [ ] **단위 테스트**: 규칙 엔진은 엣지 케이스가 많으므로 테스트 필수
  - 광대 섞인 조합, 광대 단독 플레이, 전원 패스, 마지막 카드 처리 등

### Phase 2 — 서버 + 실시간 통신
- [ ] Socket.IO 서버, 방 생성/입장/퇴장 (6자리 방 코드)
- [ ] 게임 시작 → 분배 → 턴 진행 전체 플로우 서버 구현
- [ ] 손패 비공개 전송 (플레이어별 payload 분리)
- [ ] 채팅 서버 로직: `chat:send` 검증(길이/레이트 리밋) → `chat:message` 브로드캐스트, 방별 최근 메시지 버퍼
- [ ] 시스템 메시지 발행 (입장/퇴장/게임 시작 등 게임 이벤트 → 채팅 스트림)
- [ ] 재접속(세션 복구) 처리 — 최근 채팅 내역 포함 복구

### Phase 3 — 클라이언트 UI (MVP)
- [ ] 홈 화면: 닉네임 입력, 방 만들기/참가하기
- [ ] 로비 화면: 참가자 목록, 방장의 시작 버튼, 게임 옵션
- [ ] 채팅 UI: 로비/게임 공용 채팅 패널 (데스크톱 사이드 패널, 모바일 토글 오버레이), 시스템 메시지 스타일 구분, 새 메시지 뱃지
- [ ] 게임 화면:
  - 내 손패 (카드 선택 → 내기/패스 버튼)
  - 테이블 중앙: 현재 필드 카드, 턴 표시
  - 다른 플레이어: 좌석 배치, 남은 장수, 계급 뱃지
- [ ] 라운드 종료 화면: 순위 및 다음 라운드 계급 안내

> **MVP 완성 기준**: 혁명/세금 없이 "카드 내기 → 순위 결정"의 기본 트릭 게임이 여러 브라우저에서 동작

### Phase 4 — 달무티 고유 규칙 완성
- [ ] 세금(Taxation) 단계 UI + 서버 로직
- [ ] 혁명/대혁명 선언 플로우
- [ ] 계급에 따른 좌석 재배치 (왕이 상석)
- [ ] 다라운드 진행 + 누적 점수판

### Phase 5 — AI 봇 시스템
- [ ] `BotStrategy` 인터페이스 + 봇 턴 실행 파이프라인 (랜덤 지연, 규칙 엔진 검증 통과)
- [ ] easy 봇: 유효 수 무작위 선택 (봇 플로우 전체를 검증하는 기준 구현체)
- [ ] normal 봇: 약한 조합 우선 소진 휴리스틱, 세금/혁명 기본 판단
- [ ] hard 봇: 카드 카운팅, 전략적 패스, 광대 기대가치 판단
- [ ] 로비 UI: 봇 추가/제거, 난이도 선택 드롭다운, 난이도 뱃지 표시
- [ ] 이탈 플레이어 자리를 봇으로 대체하는 옵션
- [ ] 봇 전략 단위 테스트 + 봇끼리 자동 대전 시뮬레이션으로 난이도 간 승률 검증 (hard > normal > easy 확인)

### Phase 6 — 완성도 & 배포
- [ ] 카드 애니메이션 (내기/분배), 사운드 효과
- [ ] 턴 타이머 (시간 초과 시 자동 패스)
- [ ] 퀵챗(빠른 문구 버튼) 및 채팅 다듬기 (이모지, 알림음)
- [ ] 모바일 반응형 최적화
- [ ] E2E 테스트 (Playwright, 멀티 브라우저 시뮬레이션)
- [ ] 배포 (프론트 + 백엔드) 및 환경변수/CORS 정리

### Phase 7 — 선택 확장 (여유 시)
- 관전자 모드
- 계정/전적 저장 (DB 도입: PostgreSQL 또는 Supabase)
- 공개 방 목록 / 매치메이킹
- 봇 난이도 추가 튜닝 (예: 시뮬레이션 횟수 조절로 세분화된 난이도)

---

## 6. 예상 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 규칙 엔진 버그 (특히 광대 조합) | Phase 1에서 순수 함수 + 촘촘한 단위 테스트로 격리 |
| 동시성 문제 (동시에 카드 내기) | 서버 단일 스레드에서 턴 검증, 요청 순서 직렬화 |
| 연결 끊김으로 게임 중단 | 재접속 복구 + 턴 타이머 + 자동 패스 |
| 하우스 룰 논쟁 | 방 옵션으로 규칙 변형 선택 가능하게 설계 |
| 상태 불일치 (클라 ↔ 서버) | 매 이벤트마다 전체 공개 상태 스냅샷 전송 (게임 규모가 작아 비용 미미) |

---

## 7. 검토 백로그 (리뷰에서 발견, 의도적 보류)

| 항목 | 내용 | 처리 시점 |
|---|---|---|
| 광대 마무리 금지 하우스 룰 | 마지막 카드를 광대로 내는 것을 금지하는 변형 룰 → 방 옵션 후보 | Phase 6+ |
| 엔진 직렬화(toJSON/fromJSON) | 서버 프로세스 재시작 후 게임 복구용. 현재는 인메모리 생존 전제 | Redis 도입 시 |
| shared 패키지 빌드 산출물 | 현재 raw TS export — 서버는 tsx 런타임, 웹은 Vite 번들로 소비 | 배포 구성 시 재평가 |
| enumeratePlayableCombos 광대 변형 | (n장, 광대 j장) 조합이 전략적으로 구분되는 수라 의도적으로 모두 열거 | hard 봇 구현 시 프루닝 검토 |

## 8. 권장 진행 순서 요약

1. **Phase 1의 규칙 엔진부터** 시작 — UI 없이도 테스트로 게임이 "돌아가는" 것을 검증
2. Phase 2~3으로 최소 플레이 가능한 MVP를 빠르게 완성 (혁명/세금 제외)
3. Phase 4에서 달무티 고유 규칙을 얹어 완전한 게임으로
4. Phase 5에서 난이도별 AI 봇 추가 (전체 게임 플로우가 완성된 뒤라 봇이 모든 규칙을 다룰 수 있음)
5. Phase 6에서 다듬고 배포

각 Phase는 독립적으로 커밋/PR 단위가 될 수 있도록 구성했다.
